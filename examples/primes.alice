#!/usr/local/bin/alice
"strings.alice" include
"io.alice" include

"":primes
"generate all primes to: "P

read-num
{
    d
    IF prime? {
        d primes "\n" rot rot 3 concatn
        :primes
    } FI
    --
} while

primes "primes.txt" writef
